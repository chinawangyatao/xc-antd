{
  "name": "xc-antd",
  "private": true,
  "version": "0.0.6",
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "bun run --filter @xc-antd/docs dev",
    "build": "bun run --filter @zhilv/xc-antd build",
    "build:docs": "bun run --filter @xc-antd/docs build",
    "lint": "eslint .",
    "test": "bun test packages/xc-antd/tests",
    "pack:private": "bun run test && bun run --filter @zhilv/xc-antd typecheck && npm pack --dry-run --workspace @zhilv/xc-antd",
    "publish:private": "bun run test && bun run --filter @zhilv/xc-antd typecheck && pnpm version patch --no-git-checks && npm publish --workspace @zhilv/xc-antd",
    "preview": "bun run --filter @xc-antd/docs preview"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^24.12.3",
    "eslint": "^10.3.0",
    "eslint-plugin-react-hooks": "^7.1.1",
    "eslint-plugin-react-refresh": "^0.5.2",
    "globals": "^17.6.0",
    "typescript": "~6.0.2",
    "typescript-eslint": "^8.59.2"
  },
  "dependencies": {
    "@ant-design/icons": "6.x"
  }
}
